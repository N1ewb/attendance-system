{
  "name": "attendance-system",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev --prefix client\" \"python -m uvicorn server.main:app --reload --host 0.0.0.0 --port 8000\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "python -m uvicorn server.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
